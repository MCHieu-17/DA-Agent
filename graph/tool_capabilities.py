"""Shared capability descriptions for planning and tool selection."""

CHART_TOOL_LIMIT = "The built-in create_chart tool produces one chart type on one axes in one PNG; it cannot create subplots, custom layouts, annotations, or combine chart types."
